using UnityEngine;
using System.Collections;

public class getWall : MonoBehaviour {
	public GameObject wall;
	// Use this for initialization
	void Start () {
	}
	
	// Update is called once per frame
	void Update () {
		GameObject prefab;

		//タッチされたとき
		/*if (Input.GetMouseButtonDown (0)) {
			GameObject wall = Instantiate (prefab, transform.position, transform.rotation) as GameObject;
			wall w = wall.GetComponent<wall>();

		}*/
	}
}
